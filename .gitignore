# Local configuration and credentials
.env
.env.*
!.env.example
*.pem
*.key
# Dependencies and generated application output
node_modules/
.next/
dist/
*.tsbuildinfo
build/
!docs/build/
coverage/
*.log
# Local system/editor state
.DS_Store
Thumbs.db
.idea/
.vscode/
# Local development data: the DATA_DRIVER=local SQLite file and object store (LOCAL_DATA_DIR)
.local-data
# The e2e suite's own api: its data directory and the secrets generated for that one run
.local-e2e/
# Local deployment state
.wrangler/
.vercel/
# Test output
playwright-report/
test-results/
# Private local deployment decisions and env checklist
symplist_env_and_decisions.csv
# Trigger.dev local build output
.trigger
# Next.js generated type references (regenerated by `next typegen` and `next build`)
next-env.d.ts
