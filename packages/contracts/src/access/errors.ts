import { defineErrorCodes } from "../common/errors.ts";

/** Stable error codes owned by the access feature (§5, §6), mapped to HTTP statuses. */
export const accessErrorCodes = defineErrorCodes({});
