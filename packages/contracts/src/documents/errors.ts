import { defineErrorCodes } from "../common/errors.ts";

/** Stable error codes owned by the documents feature (§9, §6), mapped to HTTP statuses. */
export const documentsErrorCodes = defineErrorCodes({});
