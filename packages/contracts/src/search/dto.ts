/**
 * REST request and response schemas owned by the search feature (§10.1 and §10.2).
 * Export Zod schemas with a `search`-specific name so the contracts index stays collision free.
 */
export {};
