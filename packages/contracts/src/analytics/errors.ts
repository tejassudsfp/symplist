import { defineErrorCodes } from "../common/errors.ts";

/** Stable error codes owned by the analytics feature (§15, §6), mapped to HTTP statuses. */
export const analyticsErrorCodes = defineErrorCodes({});
