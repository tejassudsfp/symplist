/**
 * Analytics event allowlist schemas and the payload scrubber (§15). Browser-safe: nothing reachable
 * from this entry point may import `node:*`. The server emitter is `@symplist/analytics/server`.
 */
export {};
