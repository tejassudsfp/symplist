# Trigger.dev development secret key (Dashboard > API keys > Development). Starts with tr_dev_.
# Needed to trigger tasks from code; `trigger dev` itself authenticates through `trigger login`.
TRIGGER_SECRET_KEY=
