import type { ReactNode } from "react";

/** Sign-in, account creation and email verification. Analytics never loads here (§15). */
export default function AuthLayout({ children }: { children: ReactNode }) {
  return children;
}
