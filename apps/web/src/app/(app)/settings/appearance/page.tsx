import { AppearanceSettings } from "@/features/workspace/appearance-settings";

/** Settings → Appearance: style, accent and brightness (settings_appearance.md, note 02). */
export default function AppearanceSettingsPage() {
  return <AppearanceSettings />;
}
