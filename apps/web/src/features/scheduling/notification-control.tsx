"use client";

/**
 * The notification control in the shell's top bar slot (§12.3, notifications.md). PLACEHOLDER:
 * renders nothing until the scheduling feature implements it.
 */
export function NotificationControl() {
  return null;
}
