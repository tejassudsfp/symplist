"use client";

/**
 * The analytics consent banner, mounted once in the signed-in shell and never in excluded route
 * groups (§15). PLACEHOLDER: renders nothing until the analytics feature implements it.
 */
export function ConsentBanner() {
  return null;
}
