"use client";

/**
 * The command palette, mounted once inside the shell's action registry (§10.1, §10.2,
 * command_palette.md). PLACEHOLDER: renders nothing until the search feature implements it.
 */
export function CommandPalette() {
  return null;
}
