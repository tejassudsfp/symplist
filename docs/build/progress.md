# Build progress

Checkpoint file for the end-to-end build. Read this first when resuming; update it at the end of every iteration.

**Goal:** build and verify the complete Symplist free closed-beta application from its product notes, screen briefs, and supplied UI sample, with real backend integrations, documented self-hosting, and evidence for the acceptance criteria.

- Branch: `feat/symplist-build` (no upstream; never push to `main`)
- Scope sources: [notes](../notes/files/00_index.md), [screen briefs](../../design/mockups/overall.md), [UI sample](<../../design/UI sample/README.md>), [decisions](decisions.md), [coverage ledger](coverage.md)
- Local runtime: Homebrew Node 24 (`export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`), pnpm 12.4.2
- Secrets: the owner-supplied ignored `.env.local` has been distributed into the three runtime files and verified against the placement matrix; all are mode 600. Never print values.

## Phases

| Phase | Content | Status |
| --- | --- | --- |
| 0 | Workspace scaffold, Trigger.dev worker with a registered healthcheck task | Done |
| A | Research: verify latest stable versions and current APIs for every dependency | Done (`docs/build/research/`) |
| B | Architecture and contracts: repository layout, D1 schema, API/WebSocket protocol, shared contracts, test conventions | Done ([architecture](architecture.md) revised after 56-issue adversarial review, verified) |
| C | Foundation: apps and shared packages, configuration, storage/crypto/email adapters, auth guard skeleton, theme tokens, CI | Done (C0, C-a, C-b, C-close, code review c7 fixes; independent verification 39/43 PASS, the other 4 deferred below) |
| D1 | Feature wave 1: identity/access/admin, workspace/tasks, appearance, documents/Git, keyboard/search core | Done (four branches merged into `feat/symplist-build`; every gate green, see the log) |
| D2 | Feature wave 2: Simon/executors/Composio/quick chat, scheduling/notifications/calendar, Vault, sharing/handoff, connections/MCP, analytics/consent | Implementation complete — all feature areas, Simon connector/Vault/Sharing/scheduling wiring, screens, maintenance, search contributors and carried contract/secret/load suites are integrated; current-head publication gates and browser evidence are tracked in E |
| E | Integration, end-to-end flows, visual verification at 1440/1024/390 across themes | In progress — environment placement, 45 live migrations, D1 load, executor edge matrix, bounded live provider suites and the earlier 36-frame populated-workspace matrix are recorded; current merged cross-feature browser execution and a real durable Simon relay journey remain |
| F | Adversarial review and fixes | Pending |
| G | Documentation, spec updates, self-hosting guide, pull request | Pending |

## Log

- 2026-09-20: **D2 implementation is integrated; Phase E evidence was reconciled against the merged tree.** The Simon approval, Connections/Agent Connections, system-state, cookie-consent, MCP OAuth-consent and Quick Chat screens are implemented with local component/API contracts; document, Simon-message, privacy and scheduling search contributors are registered. The executor matrix runs the same turn/native/document/Sharing/Connections paths under local and Trigger claims and now explicitly covers both-mode relock/wrong-executor refusal, concurrent duplicate submission and local↔durable active/pending-work switches. Local mode is asserted to make no Trigger calls and durable Nest installs no model/tool handler. The remaining executor proof is an executed browser → live `simon-run` → encrypted worker/API relay journey; no current-head browser/e2e or visual rerun is claimed.
  - Live bounded suites run with the ignored credentials: D1 15/15, R2 10/10, Trigger executor contract 15 passed with three intentionally skipped live-control cases (local/fake/live targets all exercised), OpenAI 12/12, Composio seven passed with five intentionally skipped target-capability cases and its bounded live probe passing, and PostHog 1/1. No live Resend delivery/webhook result is claimed.
  - `pnpm env:check` passed over 82 API, 55 worker and five web assignments; all three ignored files are mode 600. The live migrator reported zero applied, 45 already applied and zero out of order. Connections hosted-link and MCP key/OAuth code/access/refresh routes now have per-route one-time-secret scans. The combined D1 load contract remains five passing local request-budget/safeguard tests, not a live capacity or latency claim.

- 2026-09-20: **Hourly MCP and durable connection-revocation maintenance are integrated.** Shared cleanup now expires OAuth server records under the current local/durable generation fence, while the Trigger cleanup drains bounded Composio revocations through the same shared reconciler used by the daily task. The adapter was moved to worker infrastructure so importing it never registers another Trigger task. Sixteen focused core/worker tests pass, including both executors, stale mode/generation refusal and the existing 20-request full-page cleanup ceiling; focused lint and both project typechecks pass.

- 2026-09-20: **Phase E environment distribution merged at `e87bda2`.** `pnpm env:distribute` now splits ignored root `.env.local` into the three app files from the authoritative templates plus §4.5 inventory, validates all runtime schemas and shared-family equality before atomic mode-600 writes, and reports counts only. `pnpm env:check` re-validates placement, unknown-variable exclusion, schemas, shared versions/values and file modes. Eight adversarial script tests and 225 focused config tests pass; full lint and docs checks pass. A live-shaped run against the owner's ignored values produced and checked 82 api, 55 worker and five web assignments only after normalizing a private copy's stale `AI_PROVIDER_MODE` to the binding `live` value. The original ignored master file was not changed and must receive that one non-secret correction before the integrated command runs. No credential value entered output, git or a test fixture. The completed worktree and local/remote WIP branch were removed after the merge was pushed.

- 2026-09-16: **Paused after merging the ready visual branch at `92d4cbd`.** All 41 branch paths accounted for: 39 byte-identical, CSS and decisions preserve every added line alongside root's newer additions. All 17 typechecks, lint over 1,303 files (zero warnings/errors), and docs check pass. The 36 frames are branch-run populated-workspace evidence, not a rerun of the new Simon UI. Vault, Scheduling/Resend, Sharing/Analytics and load-test branches were already merged. Connections `ed5abf3` remains separate pending final review/gate refresh; Connections UI `5341297` waits on it. Maintenance `e61264b` is explicitly unfinished. Root and every feature/worktree branch are saved on origin; no main push or PR. All agents are stopped. Continue from the [checkpoint report](reports/usage-limit-checkpoint.md), not by assuming D2/E complete.

- 2026-09-16: **Usage-limit checkpoint: work saved, NOT a completed D2/E gate.** Root resumes from the Simon UI snapshot described in [checkpoint report](reports/usage-limit-checkpoint.md). The owner explicitly superseded the earlier no-push rule to back up feature branches; no main push or PR is authorized. Active Connections and maintenance writers were told to stop expanding scope, snapshot their own trees and push their branches. Completed Connections UI and 36-frame visual branches are still separate and must be merged sequentially with loss audits. All 17 root typechecks pass for the current UI, and 25 focused chat tests passed before the final pagination addition. Full tests failed: two shell-layout expectations/setup errors and several core test timeouts; details and remaining work are recorded in the report. Live credentials/migrations remain untouched. Resume implementation only after checking branch heads and checkpoint reports; do not mistake saved work for verified completion.

- 2026-09-16: **Quick-chat close/expiry implemented and focused verification passes.** Owner close folds its result with synchronous encrypted-history deletion, retains only cancelled dispatch ids for repeatable cancellation, and denies late executor output. Fifteen core quick-chat tests cover save/close, rollback, ownership and expiry; four real HTTP tests cover save/close replay, CSRF, relock and cancellation outage. A retained Simon document actor now refreshes its expiry timestamp when a service constructs the deciding guard after external work, with a real quick-chat regression. All 17 typechecks and zero-warning lint pass; full tests are running. The Git-isolation repair is merged with an exact three-file loss audit. Reviewed Sharing actor-freshness fixes and Connections UI are ready in their isolated trees; incoming MCP remains in progress. Per the owner's suggestion, inspected installed AI SDK React and current Vercel AI Elements; adapting selected presentation primitives next, retaining Symplist transport/approvals. A separate writer is now building the 36 populated-workspace theme/viewport evidence frames. No live credentials or migrations touched.

- 2026-09-16: **Quick-chat save is atomic and owner-only.** Task creation, conversation conversion and historical run attachment commit together, preserving encrypted messages. Seven core and two HTTP contracts cover exact replay, changed-input rejection, ownership/expiry/relock, concurrent message acceptance and transaction rollback. All 17 typechecks pass. New HTTP expectations were corrected to the existing platform contract (mismatch 422; protected folded replay hides inaccessible resources with 404), not production behavior changed to suit a test. Close/expiry and the chat UI remain unbuilt. Native-tool builds, clean web build, local smoke and API deployment checks from the previous checkpoint passed. One-time-secret scans merged and their 21 focused tests pass; MCP/OAuth coverage remains with Connections. The isolated Git-fixture repair is ready to merge after this checkpoint.

- 2026-09-16: **Simon native task/scheduling adapters implemented in both executors.** A shared claimed-run session supplies trusted owner/generation/cancellation/expiry predicates to task creates, subtree moves and versioned scheduling reads/writes/replays. Task-create ids are stable per run/tool call. Local task writes use the existing cache/realtime signals; durable task changes use one ids-only relay subscriber per runtime, and schedule hints re-read the canonical owner-scoped version in the API. Twelve new core contracts include write-time races, foreign ownership, locked/paused/expired/stopped runs and replay refusal; the same five-step native tool model contract passes under both executors. Worker announcement and real API event regressions pass. All 17 typechecks and lint over 1,276 files pass. Full run passes web 1,558/core 565/agent 78/worker 95, but API repeats the pre-existing sharing fixture document-publication 500; an isolated writer is diagnosing the actual exception. Builds/smoke are running; no browser/live evidence claimed. Next checkpoint, merge the completed one-time-secret scans, then finish quick-chat lifecycle and Simon UI/remaining sharing and connector tools.

- 2026-09-16: **Load and shared maintenance merged sequentially.** `19c1834` preserves all three load-contract paths; `9e63cfc` preserves all maintenance code/tests and union decisions. All 17 typechecks pass after each merge; five load/safeguard, 13 cleanup, 32 API/realtime and one worker-adapter tests pass. The load contract measures 379 requests, not a five-minute completion SLA. Vault/share bounded fenced cleanup and canonical socket-targeted lock-before-close are now wired in both executors. Root is implementing Simon native task/schedule tools; Connections backend continues, its UI now has a separate writer, and the maintenance writer is completing non-MCP one-time-secret scans. No live/environment changes.

- 2026-09-16: **Strict Mode follow-up checkpoint `a498a43` verified by package suites.** Web 1,558, core 540, agent 76, worker 87 and API 448 pass; 47 script tests pass. Two combined attempts were not fully green: the first hit a Vault key-derivation test timeout (unchanged focused 19-test rerun passed), and the second hit the previously observed sharing document-publication fixture 500 (unchanged focused 16-test and complete API reruns passed). No test, timeout or assertion was weakened. All 17 typechecks, lint and docs checks pass. Browser/build/live gates were not rerun for this lifecycle slice; D2/E remain in progress.

- 2026-09-16: **Owner's Strict Mode and search-AAD fix incorporated (`4d67768` → `0d702f5`) and extended.** Workspace and scheduling providers now have actual Strict Mode replay regressions. Scheduling reopens its memoized store, fences abandoned microtasks/responses, resets its queue and prevents old subscription cleanup deleting a reopened entry. Preferences restart cleanup-cancelled debounced saves and reconcile writes that settled while disposed. Store tests cover both pending and already-settled loads; a cross-feature regression creates a real task through TaskService and decrypts it through both search read paths, avoiding the former self-consistent but incorrect fixture AAD. Focused workspace/scheduling tests pass (131), the new search boundary test passes, all 17 typechecks and zero-warning lint over 1,262 files pass. Full suite is running; no new browser/live evidence claimed. Next merge the completed D1 load contract, then continue Simon while Connections/MCP and shared maintenance finish in isolated worktrees.

- 2026-09-16: **Task-tool transaction authorization implemented for Simon/MCP integration.** Added namespaced trusted predicates to create/move and scoped task reads; actual effects and recorded responses require fresh authority. Duplicate create and no-op move paths no longer bypass it. Review also fixed first-page-only no-op subtree reporting and stale cached IDs (whole subtree plus atomic version witness, cache invalidation on refusal), unauthorized non-fold planning-error disclosure, and a task moving between detail and subtree reads invalidating the no-op precondition. New regressions cover write races, exact replay revocation, foreign folded claims, bind collisions, cached reads, revoked archived parents and 55-child cross-instance moves. Focused 56 task tests pass; all 17 typechecks and lint over 1,260 files are green. Full suite (core 538 before the final extra no-op-precondition regression, API 448, web 1,547, remaining packages and 47 scripts), both builds, local smoke/deploy/docs pass. One earlier sharing document-save fixture returned 500 under concurrent load; its unchanged focused and full reruns pass, no test weakened. Connections will consume this seam for actual scoped MCP adapters; it is not yet wired into Simon native tasks. Browser baseline remains the preceding 107-pass combined run. Next incorporate the owner-provided Strict Mode/search-AAD fix `4d67768`, audit all merged stores with development remount regressions, and merge the completed load contract. Maintenance/Vault lock ordering now has an isolated writer; root retains Simon.

- 2026-09-16: **Combined D2 browser integration gate passes: 107 passed / 19 existing viewport skips.** Fixed a real WCAG 2.2 target-size defect in notification-setting checkboxes (24px controls, 32px rows). Updated the pre-D2 exact tab sequence to include Notifications without removing any expected focus stop; scoped the search retry to its main region now that consent has an independent retry. Fixed the new Vault browser fixture to send the real `X-Symplist-CSRF` header to the test-only OTP route, with a stronger status/body assertion. Vault setup/edit/lock/recovery, deadline/calendar/settings and saved-document/share/viewer/revocation journeys pass at 1440/1024/390. Vault captured 36 theme/mode populated-list frames as run attachments, not yet retained/reviewed visual evidence. No live provider claims. Next root slice is task-tool run/grant authorization, then remaining Simon lifecycle/UI/integrations; Connections and the isolated load contract continue in parallel.

- 2026-09-16: **Sharing/Analytics integrated after Scheduling.** Per-file audit accounts for all 77 branch paths (69 byte-identical, eight intentional integration differences), preserving all appended CSS, implemented shell controls and the stronger analytics queue regression. Frozen install, all 17 typechecks, zero-warning lint over 1,259 files and 165 focused core/HTTP/UI/analytics tests pass. Full combined tests pass (web 1,547; core 526; API 448 with six existing live skips; agent 76; worker 87; remaining packages and 47 scripts). Both builds, local smoke, API deploy with 40 migrations and docs check pass. Combined browser/visual/live evidence is not yet claimed. Connections remains active. A new isolated `wip/e-d1-load` writer builds the actual ten-turn/fifty-reminder D1 load contract from the Scheduling-integrated checkpoint while root finishes integration and Simon.

- 2026-09-16: **Scheduling/Resend integrated after Vault.** Both conflicting shell assertions and full CSS blocks are preserved; reviewed all 81 branch paths (73 byte-identical, eight intentional integrator differences). Frozen install, all 17 typechecks and 95 focused core/API/UI tests pass. Reduced unnecessary Vault list ancestor specificity exposed by combined lint. Native Simon registration, feature cleanup and reminder analytics remain integration work; browser/live/visual evidence is still pending. Next merge Sharing/Analytics separately.

- 2026-09-16: **Vault branch integrated after Simon checkpoint `09960ab`.** No conflicts; per-file audit accounts for all 54 branch paths (50 byte-identical, four retaining newer integrator fixtures/docs). All 17 typechecks and focused 49 core/Search + 10 Vault HTTP tests pass. See [integration audit](reports/d2-integration.md). Screens are implemented, not browser/visually verified. Next merge Scheduling/Resend, then Sharing/Analytics separately; preserve shared CSS, shell slots and OTP infrastructure. Simon resolver/picker, shared lock events and hourly cleanup remain explicit integration work.

- 2026-09-16: **Simon public history and production conversation snapshots implemented.** Added owner-only paginated HTTP history and real conversation topic authorization/snapshot registration. History reads are one batch, capped by both 50 rows and 2 MiB encrypted payload; public parts omit reasoning, provider metadata and raw tool inputs. Persisted approval/question resolution cards survive projection. Snapshot live tails are restricted to the active run and capped at 256 KiB, with an explicit truncation flag. Browser reduction and the actual chat UI remain open.
  - Adversarial fixes: an authorization denial after topic authorization used to fall through to `resync` and leave the subscription live. The new explicit denial detaches that subscription and discards queued plaintext; real quick-chat expiry and queued-frame tests cover it. Added missing resolution-part projection and large-payload pagination/tail tests. Updated the three pre-D2 synthetic realtime/relay fixtures to replace only Simon topic registration, retaining their existing ownership/replay assertions; real production ownership now has its own HTTP/WebSocket tests.
  - Verification: frozen install, lint over 1,152 files with zero errors/warnings, all 17 typechecks, full tests and 47 script tests, both builds, local smoke, API deploy and docs checks pass. Focused history/realtime/platform tests pass after the final denial guard. Browser e2e is 95 passed / 19 existing viewport-specific skips on alternate ports (`E2E_WEB_PORT=3300 E2E_API_PORT=4300 NEXT_PUBLIC_API_URL=http://127.0.0.1:4300 NEXT_PUBLIC_WS_URL=ws://127.0.0.1:4300`); the separate `symplist-run` dev server occupying 3000 was left untouched. A subsequent isolated menu-Escape failure passed on the unchanged full rerun. Evidence remains D1 regression frames, not completed D2/E visuals.
  - Gate repair: the analytics queue test repeatedly assumed `setTimeout(0)` settled an in-flight SDK gzip. Read-only delayed-compression reproduction showed old batch IDs arriving after simulated recovery. The test now awaits an offline flush barrier and asserts exactly the newest three IDs, stronger than its former upper-bound assertion. Production analytics was unchanged; the full suite passed after this synchronization correction.
  - **Resume here:** checkpoint Simon, then merge ready Vault (`f881976`), Scheduling/Resend (`1792f5f`) and Sharing/Analytics (`e2b61f0`) sequentially with typechecks and per-file loss audits. Connections/MCP remains active. Then finish Simon UI/live reduction, quick-chat close/save/cleanup, remaining native/wrapper tools and edit-validator/Vault integration, status announcements, history compaction and unavailable-provider states. All Phase E live, visual, combined journeys and D1 load work remains pending; no live migrations or environment distribution yet.

- 2026-09-16: **Simon approval HTTP decisions implemented and reviewed.** Owner-only approval reads and fresh-session decision routes now fold decision/continuation or edited-draft replacement with the encrypted idempotency result. Preparation uses one bounded batch; duplicate requests return the exact recorded result without another continuation or catalogue validation. Edits still require a new approval. The metadata-only validator factory is intentionally awaiting Connections integration and fails closed when absent; this is not a finished approval UI.
  - Adversarial fixes: a cached revoked session could approve, and an exact recorded response could replay after task archive. Added fresh route admission, a deciding/replay session-revocation predicate and active-task replay guard, with regressions. Ordinary approval also rechecks connection generation; a disconnect/reconnect or account crypto-shred during preparation cannot commit. The older preliminary-read race test now intercepts the actual batched read rather than the public `load` method, preserving the archive-race assertion.
  - Verification: frozen install, lint over 1,147 files with no errors/warnings, all 17 typechecks; full suite passes with core 422, API 398, web 1,484, agent 76 and worker 82, other packages and 47 script tests. Both builds, local smoke, API deploy and docs checks pass; 95 browser tests pass with 19 existing viewport-specific skips. Browser frames remain D1 regression evidence, not D2/E visual coverage. Existing credential-gated live skips remain.
  - Parallel: Sharing/Analytics is complete on its branch at `e2b61f0`, clean, with local gates green; Vault remains ready at `f881976`. Scheduling has passing sequential full-suite checks and final build/report work; Connections is building lifecycle/MCP/OAuth after committing live catalogue/session wrappers and the metadata approval-validator seam. Nothing is merged, pushed or live-migrated yet.
  - **Resume here:** build conversation history/realtime snapshots and full Simon/quick-chat UI/lifecycle, finish remaining native tools and integrations, history compaction and unavailable-provider presentation. Integrate the completed worktrees sequentially with typechecks/loss audits, including the owner-bound edit validator, Vault resolver/cleanup, scheduling tools, sharing maintenance and analytics. Phase E cross-feature, visual, live and load verification is still pending.

- 2026-09-16: **Simon question routes and incoming-actor transaction seam implemented.** Owner-only question read/answer/dismiss routes now fold answers and continuations with the idempotency result; normal chat submissions remain queued and cannot answer a question. Tests cover racing answers, stable replay/mismatch, dismissal, foreign/expired reads, CSRF, encrypted question/answer persistence, completion rollback and crypto-shred between preparation and commit. The optional trusted `SimonWriteFold.authorization` and run-read predicate let MCP supply fresh grant/task-scope checks inside Simon SQL; bind names are isolated and replays cannot bypass that authority. The real MCP grant builder remains owned by the Connections stream.
  - Verification: lint over 1,146 files with no errors/warnings; all 17 typechecks; full suite passes (core 411, API 393, web 1,484, agent 76, worker 82 and the other packages/47 script tests). Both builds, smoke, deployment and docs checks pass. A subsequent bind-isolation hardening is covered by the 21-test focused fold suite and lint. Browser e2e passes again (95 tests, 19 existing viewport-specific skips); generated frames remain D1 regression evidence, not a new question/chat-UI journey.
  - Parallel transition: Vault finished at `f881976`, clean, with branch gates green and browser specs authored but unrun; its integration/report remains in `symplist-wt/vault`. Connections/MCP is now actively building in `symplist-wt/connections`, branched from `8e26f4f`, using the freed slot. Scheduling has UI checkpoint `3e98dca` and a reviewed backend follow-up in progress; Sharing/Analytics is running its full gates after repairing pre-D2 probe route collisions.
  - **Resume here:** main owns Simon. Finish approval HTTP folding/edit validation, history and realtime snapshots, quick-chat close/save/cleanup, full chat/approval UI, remaining tools and adapter integration, context-epoch compaction and unavailable-provider states. Integrate other branches sequentially with loss audits and typechecks; wire Vault resolution/cleanup, scheduling actor tools and cleanup seams, sharing maintenance, analytics and Connections/MCP. Then all Phase E live/visual/journey/load checks. No live environments or migrations have been distributed/applied, and no push or PR exists.

- 2026-09-16: **Simon HTTP command slice implemented; D2 remains in progress.** Added admitted app-class create-conversation, send-message, run-status, Stop and explicit Retry routes. Creation, acceptance, Stop and Retry fold the platform claim and encrypted result into their deciding transaction. Accepted/queued outcomes are recorded correctly under concurrent submission; a permanent message request id still deduplicates after the 24-hour response record expires. Stop persists before safe repeated executor cancellation. Retry creates a linear child at the current executor generation without displacing active or queued work.
  - Review fixes: conversation creation no longer returns an existing archived task conversation; a relock, archive or account-key deletion after preparation cannot record a successful message or replay a protected response. A queued message advancing while an expired response is being reconstructed refuses that stale reconstruction. Retry resolves the exact original pause across multiple interruptions, including a question expired by Stop, and reads succeeded/uncertain external actions from their ledger without sending again. Migration 0502 adds only bounded-lookup indexes.
  - Verification: frozen install; lint over 1,145 files with zero errors/warnings; all 17 typechecks; full tests (core 407, API 391, agent 76, worker 82, web 1,484, other packages and 47 script tests); both builds; 95 browser e2e passes with 19 existing viewport-specific skips; local smoke; deploy check over 34 migrations; docs and whitespace checks. The first full test run hit three existing Git-test timeouts during competing builds; all 28 tests in those two files passed in isolation and the unchanged complete suite passed on rerun. Nine existing credential-gated live skips remain. Regenerated browser screenshots are D1 regression evidence, not new Simon UI or Phase E visual coverage.
  - **Resume here:** finish approval/ask HTTP idempotency, conversation history/realtime snapshots, quick-chat close/save/expiry and all Simon UI; then remaining native tools and Composio adapter integration, context compaction/epochs and missing-provider presentation. Vault's branch gates are green but its final checkpoint/report is pending; scheduling and sharing branches have backend checkpoints and ongoing UI/review work. Connections/MCP takes the next freed agent slot. Merge branches sequentially with typechecks and loss audits, then complete all Phase E flows, live environment/migration checks, visual matrix and load tests. Nothing has been pushed or live-migrated.

- 2026-09-16: **Simon runtime/document-tool slice verified locally; no D2 feature is complete yet.** Both adapters now call the shared loop with native task-context/document tools. Durable Git goes through the existing encrypted R2 job bridge and a different queue; local Git shares the API's process-wide two-slot infrastructure service. Native read receipts and retrieval counts commit with encrypted tool-result snapshots. Stable document conflicts reach the model, while arbitrary provider/tool errors do not.
  - Added the real-loop §8.3 marker test covering a user message, persisted document read, arguments/results, hidden reasoning, encrypted signed output push, metadata/logs and a provider failure through the fake Trigger recorder. This is a local contract test, not a live Trigger verification. Added the same native document read → receipt/checkpoint → edit → stale-revision conflict contract under local and durable executors, with real encrypted job objects and the child Git handler.
  - Review fixes: Git children now match the exact live parent claim and include cancellation in final publication guards; a Stop arriving after a read but before its tool-result checkpoint no longer records a false read receipt. A structural test caught an attempted cross-feature Nest import; moved only shared Git concurrency into infrastructure and retained independent core-service construction. The pre-D2 job fixtures now create real claimed Simon runs, and the shared task fixture uses valid encrypted titles rather than a dummy envelope.
  - Verification: lint over 1,138 files with no errors/warnings; all 17 projects typecheck; full unit/contract/script suite passes (core 377, agent 76, API 379, worker 82, plus existing projects and 47 script tests). Nine pre-existing live skips remain. Both builds pass. The clean-web gate removes package outputs, so rebuilt them before the successful local smoke. API deploy check passes with 33 migrations; docs check and diff whitespace check pass. Browser e2e has not been rerun for this slice; the prior full run remains the recorded 95 passes, not new feature evidence.
  - **Resume here:** main checkout owns Simon only; Vault, Scheduling/Resend and Sharing/Analytics agents are live in their isolated worktrees. Do not edit those trees. Next finish Simon HTTP/idempotency/realtime routes, retry/quick-chat lifecycle and UI, remaining native task/schedule/sharing tools and Composio wrappers; review bounded history/context epochs and adapter-level mode isolation. Connections/MCP starts in the next available agent slot. Merge Simon first, then the other streams individually with typecheck and loss audits, then all Phase E flows/live checks/visuals/load tests. No live migrations or environment distribution has occurred.

- 2026-09-16: **Parallel D2 work authorized by the owner's latest instruction.** See [stream ownership](d2-parallel.md). Simon's shared model loop, provider registry, encrypted structured checkpoints and local/Trigger adapters are checkpointed but not fully verified: lint and all typechecks passed, focused agent/core/worker tests passed, but the full suite exposed legacy internal/platform test fixtures that omit Simon's execution definition. Updating those fixtures is in progress; the first adjustment duplicated the account-purge definition and still needs correction. This is an explicitly unverified checkpoint, not a completed feature. Vault, Scheduling + Resend and Sharing + Analytics now branch from this checkpoint in isolated single-writer worktrees. Full tools, HTTP/UI integration, marker test and Phase E remain open.
  - The three isolated writers are running. The pre-D2 fixture correction now retains only Simon's real execution definitions (the runtime already retains account purge separately). API suite passes: 34 files, 379 tests, 6 existing live skips. No assertion was removed. Other full gates remain pending for this slice.

- 2026-09-16: **D2 started in the single clean build checkout.** The owner's current instruction supersedes the old worktree/attribution instructions: no worktrees, pushes, PRs or credit trailers. `CLAUDE.md` and `AGENTS.md` still describe the earlier D1 crash; the verified D1 log below and clean checkout are authoritative.
  - First implementation slice: migration `0500_simon.sql` adds encrypted conversation/message storage, runs, approvals, asks and the invocation ledger. `SimonRepository` implements conditional message acceptance, duplicate request reconciliation, bounded queues, executor/generation claims, step snapshots, stop and atomic oldest-message advancement. Archive, restriction and account purge now include Simon's data and cancellation state.
  - `packages/agent` now contains versioned mandatory rules and a deterministic action policy. No connector action is exempt until its schema and selector paths have been explicitly reviewed. Mixed approval batches and sandbox/meta execution slugs are refused; untrusted content cannot close its data delimiter.
  - Verification: `pnpm install --frozen-lockfile`; lint over 1,115 files with zero errors/warnings; all 17 projects typecheck; 3,705 unit/contract tests plus 47 script tests pass (9 pre-existing live tests still skip pending Phase E distribution); both builds; 95 e2e passes (19 viewport-specific skips); local smoke; api deploy check with 31 migrations; docs check. Agent has 32 tests and core has 299, including 32 Simon persistence/cross-domain tests. The e2e run regenerated existing D1 screenshot evidence; it is not Phase E visual coverage.
  - Review fix: the initial claim needed an explicit account-key existence condition. Without it a missing key could leave a run marked running while returning no claim to its executor. The conditional update now refuses it before changing state, with a crypto-shred regression test. Checked queue races, generation fencing, owner-bound foreign keys and bounded purge ordering; no existing test was weakened or suppressed.
  - Second implementation slice: encrypted approval/question pauses and conditional continuations, edited-draft reapproval, exact owner/account/generation authority, an at-most-once external-action ledger, bounded expiry reconciliation and Simon's real lifecycle tracker/relay source. The minimal confirmed-connections table is pulled forward for real authorization; no OAuth or connector catalogue implementation is claimed.
  - Second-slice adversarial fixes: late dispatch cannot rebind a running old-generation run; expiry must match the pause's own run; ineligible expired pauses cannot monopolize the bounded sweep; self-hosted queue advancement respects its disabled beta gate; the approval-generation addition is nullable with tested dual-read rather than a non-null schema change. Regression tests cover simultaneous decisions, reconnect races, lost claim/results, restart uncertainty, Stop during a side effect and 201-id heartbeats. The pre-D2 test expecting an absent relay source is updated for the registered implementation, not skipped or weakened.
  - Second-slice verification: lint over 1,123 files with zero errors/warnings, 17-project typecheck, 3,767 unit/contract tests plus 47 script tests, both production builds, 95 e2e passes (19 existing viewport-specific skips), local smoke, api deployment with 33 migrations and docs check all pass. Core now has 361 tests, including 39 persistence/lifecycle and 55 approval/ask/ledger tests. The nullable-generation regression exposed SQLite expression affinity for string-bound numbers; the deciding guard now explicitly casts the generation. Live tests still await Phase E. Existing D1 screenshots regenerated by the gate are not new D2/E screen coverage.
  - **Resume here:** this is still a foundation slice, not completed Simon. Next implement the shared AI SDK loop and provider registry, document job bridge, `simon-run`/local integration, HTTP idempotency/realtime routes, retry and quick-chat lifecycle, and task/quick-chat UI. Wire pause reconciliation into the local/durable schedulers. Then D2b–g, followed by every Phase E item. No live migrations or environment distribution has happened yet. Do not mark any D2 screen or the phase verified from these unit tests.

- 2026-09-15: Full read of repository, notes, 44 briefs and UI sample. Deployment and product decisions confirmed with the owner (see [decisions](decisions.md)). Scaffolded pnpm workspace and `apps/worker` (Trigger.dev 4.6.0); worker typechecks, 5 tests pass, local worker registered with the Trigger development environment.
- 2026-09-15: Phase A research completed by 10 parallel agents (versions locked in [architecture](architecture.md) section 1). Key findings: TypeScript 7.0.2 works across the stack except the Nest CLI and typescript-eslint (use tsc and Biome); Cloudflare API rate limit (~1,200 requests/5 min) constrains D1 REST usage; OpenAI models `gpt-5.6-luna` and `gpt-5.6-terra` verified. Architecture drafted.
- 2026-09-15: Phase B complete. Three critics (spec coverage, implementability, security) raised 56 issues (8 blockers); all applied per integrator rulings recorded in [decisions](decisions.md) R1–R13, verified by an independent checker (1 gap and 11 contradictions found and fixed). Review record in `docs/build/reviews/architecture-review-1.md`.
- 2026-09-15: Phase C0 workspace foundation committed (13 packages, 4 apps, seams, CI; lint/typecheck/test/build pass; 27/27 independent checks). Phase C-a started in five worktrees under `../symplist-wt/` (contracts/config, crypto, data/storage/migrations, web shell, email/analytics/testing), each with an adversarial review pass. Phase C-b (api core; realtime, executors, worker infrastructure) follows the C-a merge.
- 2026-09-15: Phase C-a slices merged after adversarial review: contracts/config (8 defects fixed), crypto (7 fixed), data/storage/migrations (11 fixed), email/analytics/testing fakes (11 fixed). Merged tree: lint clean, 15 projects typecheck, ~1,180 tests pass (live D1/R2 suites skipped pending credentials), build and docs check pass. Phase C-b (api core; realtime, executors, worker infrastructure) started from 5ae570d.
- 2026-09-16: Owner set the current milestone: complete Phases C, D1, D2 and E on local stand-ins, merge everything into one clean `feat/symplist-build`, then pause for API keys before live checks and Phases F and G.
- 2026-09-16: Owner asked to close Phase C fully before D1: after the C-b merge, a C-close pass builds all open foundation items (pnpm dev, api Dockerfile and image test, CI job skeletons, font subsets, release-age cleanup, analytics and fake fidelity fixes, dist type resolution, Trigger re-registration, C-b leftovers) and an independent Phase C verification runs.
- 2026-09-16: **Phase C done.**
  - Summary of the phase:
    - C0 laid out the workspace: 13 packages, 4 apps, seams and CI.
    - C-a merged five slices after adversarial review: contracts and config, crypto, data, storage and migrations, the web shell, and email, analytics and testing fakes.
    - C-b built the api platform (c6: bootstrap, route classes, sessions, access, idempotency, abuse limits, hardened after a security review) and the realtime gateway, internal endpoints, executors and worker infrastructure (c7).
    - C-close wired the platform together (cz-a: seams, runtime modules, account purge runtime, executor contract suite, body limits, `LOCAL_DATA_DIR`) and the tooling (cz-b: `pnpm dev` supervisor, api image and deploy checks, CI e2e and api-image jobs, font subsets, analytics capture isolation, Composio fake fidelity, `pnpm smoke:local`).
    - Phase C verification added the Trigger config loader test and the email transport contract suite (decisions CZV.1 and CZV.2).
  - All six findings of [code review c7](reviews/code-review-c7.md) are fixed, each with a regression test that failed before the fix (decisions C7R.1–C7R.6):
    - relay key-load failures answer 503;
    - internal event replays report 409 while in progress and 200 when completed;
    - realtime access updates are monotonic;
    - local dispatch has a durable start marker (migration 0018);
    - run output dedupes outside the event id memory;
    - sign out everywhere is judged by session creation time.
  - Also added:
    - worker `d1.requests` counters every minute and at task end (decision CZV.3);
    - a structural test that every package manifest follows §2.2 point 1.
  - Independent verification: 39 of 43 checks PASS.
  - Final local run, all passing:
    - `pnpm lint` with zero warnings, `pnpm typecheck`;
    - `pnpm test`: 2,384 Vitest tests plus 47 script tests, with 9 live tests skipped for missing credentials;
    - `pnpm build`, `pnpm build:web:clean`, `pnpm install --frozen-lockfile`;
    - `pnpm e2e` (shell and smoke specs: 30 passed, 12 skipped because they run only at other viewports; evidence screenshots unchanged);
    - `pnpm smoke:local`, `node scripts/check-api-deploy.mjs` (18 migrations), `python3 scripts/check_docs.py`.
  - Deferred, with target phase:
    - AI provider and Composio wrapper contract suites: D2.
    - D1 load test (§3.1): D2/E.
    - Purge contributors for tasks, preferences, search and access: D1 feature waves.
    - Per-endpoint one-time secret scans (§6.1) and the Simon Trigger marker-string test (§8.3): D2.
    - Deploy configuration and CI `migrate`/`deploy-trigger` jobs: G.
    - Live suites (D1, R2, Trigger, OpenAI, Composio, PostHog): after credentials are provided.
- 2026-09-16: D1 incident: a mid-run reply to the workspace web-stage agent resumed it outside the workflow; it stopped with four untested web files (tree.ts, api.ts, errors.ts, task-store.ts) and no committed web stage. The workspace backend (5d14507) is intact. A dedicated workspace web and review pass runs on the existing worktree before the D1 merge. Access, documents and search pipelines are unaffected.
- 2026-09-16: Workspace worktree contention resolved by stopping the original D1 workflow's review:workspace agent, leaving the recovery workflow's web builder as the only writer. The recovery workflow's review stage covers the whole wip/workspace branch (backend and web), so the stopped backend review is not lost. Agent prompts now carry an autonomy rule (decide and record, never pause to ask), because every stall came from an agent waiting on a reply, which restarts it.
- 2026-09-16: The stopped workspace reviewer's three backend fixes were committed by hand on `wip/workspace` (4689518), touching backend paths only so the running web builder was undisturbed: migration `0202` no longer rebuilds `user_preferences` (it violated the expand-only rule and would have lost writes mid-deploy — replaced with an additive `user_preferences_panels` table, a dual read and a purge contributor for both), `planComplete` now writes a `search_intents` row for archived tasks (§10.1), and the restore-already-active path records the same access condition as the active path so a relock cannot replay a recorded success. `packages/db/src/migrations.test.ts` now rejects `DROP TABLE`, `RENAME` and `ALTER ... COLUMN` in any migration, making the expand-only rule structural. Correction to that commit's own message, which the reviewer flagged afterwards: the commit carries five fixes, not three (it also touches `packages/core/src/tasks/state.ts` and appends to `decisions.md`), and the relaxed foundation schema assertion pinned the exact *table list*, not column order — feature ranges add their own tables, so the foundation set is now asserted as a subset while every table in the database is still required to be `STRICT`. The message was left as written rather than amended, because another agent was committing on the same branch at the time.
- 2026-09-16: D1 close-out workflow started for the three remaining feature worktrees. Access (backend 46cd1cd) and search (backend c1bf3b8) had complete but uncommitted web stages; documents (backend cd1bea2, c0c314c) was left half-built when its web agent died, missing the history view, the artifact screens and all web tests. Each feature gets a finish stage and an adversarial review stage over the whole branch diff. Workspace continues in its own recovery workflow.
- 2026-09-16: Second workspace incident, opposite in shape to the first: both agents in the recovery workflow stood down as duplicates, each citing the other's writes as proof it was the redundant one. The builder had in fact written the whole web stage (~24 files, ~5,000 lines, typecheck clean) over 26 minutes before concluding it was the duplicate; the reviewer then produced a read-only report and refused to write. Nothing was lost, but nothing was committed either. Lesson recorded: an instruction to stand down if you are a duplicate is unsafe when two agents share a worktree, because the evidence looks identical from both sides. Ownership is now asserted positively in the prompt ("you are the only agent here, do not check") rather than left to the agent to infer.
- 2026-09-16: The review produced a precise work order for the workspace stage — eleven defects (a stale tree cache entry that `listArchive` proves stale and then keeps; six tree accessibility defects including per-level `aria-posinset`, a level jump in search mode and two tab stops in one `treeitem`; stale lint suppressions and 26 lint errors; a shared realtime socket that survives an account change; a rail drop target that reaches into shell DOM; leaked client idempotency keys on the 409 path) and six coverage gaps (no component tests, no workspace e2e spec, slot tests still asserting the pre-D1 world, four stub route pages, no decisions row for the platform wiring, and the two known e2e hazards). A single-owner finish-and-verify workflow now closes all seventeen.
- 2026-09-16: Third and worst contention incident, and this one was mine. I launched a close-out workflow over the access, documents and search worktrees believing the original D1 wave had ended, because `ListAgents` does not list agents spawned inside a workflow — it showed one stale subagent and nothing else. The original wave was in fact still building in all three. For about twenty minutes each worktree had two writers. The documents agent caught it and paused rather than producing a broken mix of two designs; the access agent caught it and declined to fight for the same files. The close-out workflow was stopped in full and the original wave keeps all three stages. Ownership is now one writer per worktree: workspace, access, documents, search.
- 2026-09-16: The collision also produced a real design ruling. The second documents writer had replaced the page view with a textarea per section. The original writer was right to object: §9.3 and the Phase A research decision make the page view Milkdown, and R7's "formatting normalized" commit only makes sense because the remark serializer is a fixed point of the canonical server serializer. The Milkdown design stands and the contradicting files were discarded.
- 2026-09-16: Rule for the rest of the build, learned three times over: never start a workflow over a worktree without positive proof the previous one released it, and check the workflow transcript directories rather than `ListAgents`, which cannot see workflow agents. When an agent asks who owns a worktree, answer it — an unanswered agent stalls forever, and telling agents to yield when they suspect a duplicate makes both yield.
- 2026-09-16: Every running agent was terminated at once by a session rate limit (reset 07:00 IST). Nine of the original D1 wave's fifteen agents had finished; the six remaining web and review stages died mid-flight, as did both workspace agents. Nothing was lost: each worktree's working tree was snapshotted into a WIP commit as soon as the session resumed — workspace `1658ccd` (38 files, on top of `53035d3`, which had already landed the archive tree-cache eviction, defect D1), access `e278925` (95 files), documents `5059885` (31 files, interrupted while building the history route), search `2c402d8` (81 files). Those snapshots are explicitly unverified: no check was run on them.
- 2026-09-16: All four D1 stages relaunched as one workflow with a single owner per worktree, each followed by an adversarial review of the whole branch. The prompts now assert ownership positively, forbid the peer-detection checks that cost three earlier stages, and carry each feature's own work order — for workspace that is the thirteen open defects and seven gaps from the review, for documents the Milkdown ruling and the history route, for access and search an assessment of the snapshot before finishing it.
- 2026-09-16: Two of the four D1 stages are finished and reviewed **ready to merge**. Search: the inherited snapshot was already green, so the builder fixed two defects rather than rebuilding it — a literal NUL character in a request-key template (git classifies such a blob as binary, so the file had no textual diff and would not three-way merge) and a test-support file leaking into the core build; the reviewer then fixed a freshness poll that could never terminate. Access: 215 tests across 23 files for the feature alone; the reviewer fixed three real defects (reloading the code step discarded a live OTP challenge, the leave guard failed to clear its bypass after the person confirmed, and `onComplete` fired on any full-length sanitized value) and verified each by reverting it and watching its test fail. Repo-wide the access branch runs 2,767 tests passing, 9 skipped.
- 2026-09-16: Merge hazards recorded for the integrator. `apps/web/src/components/shell/workspace.tsx` is a conflict hotspot — the search and workspace branches both modify it from a common 658-line seam. `apps/web/src/proxy.ts` gains an additive, route-agnostic session-hint redirect on the access branch. Three items are deferred to the merge because they cannot be fixed inside one worktree: the command palette's recent-tasks fan-out issues eight parallel `GET /v1/tasks/:id` calls against an endpoint the search worktree does not contain; `InviteAdminService.list` can cost about twenty D1 requests for one admin search; and keyboard remapping is inert in the search worktree alone because its `app-shell.tsx` passes no `preferences` to `ActionsProvider` — the workspace branch adds exactly that seam, so the merge resolves it.
- 2026-09-16: Documents is finished and reviewed **ready to merge**, and it was the largest of the four: the feature went from zero tests in the snapshot to 347 across 22 files, taking `apps/web` from 719 tests to 1,066. The reviewer fixed seven defects, the sharpest being a save that owed only a formatting normalization publishing it and then immediately re-publishing, a normalization publish reusing a fixed idempotency scope, `resolveConflict` hardcoding `stillDirty: false`, keyboard focus stranded on a hidden panel at the phone breakpoint, and `openRevision`/`loadMoreHunks` writing results with no stale-response guard. Known limits recorded rather than hidden: the artifact viewer, share dialog and grant list are deliberately left as the `artifact-surface.ts` seam for the sharing feature in D2, since their contracts and the api's `ARTIFACT_ORIGIN` routes do not exist in D1; and no Playwright coverage was added, so real layout, scroll preservation and the phone breakpoints are still only exercised through jsdom.
- 2026-09-16: Two more merge hazards recorded. The documents snapshot added `@codemirror/commands@6.11.1` to `apps/web/package.json`, `pnpm-workspace.yaml` and `pnpm-lock.yaml`, so the merge must re-run `pnpm install` and resolve the lockfile that way. `apps/web/src/app/globals.css` is a second conflict hotspot — the documents snapshot alone added about 780 lines there and every other worktree writes to the same file, though each branch's contribution is a pure append.
- 2026-09-16: **All four Phase D1 stages are finished and independently reviewed ready to merge.** The workspace reviewer's five fixes were the most consequential of the wave: the task tree had **no tab stop at all** — every `treeitem` took its tabindex from an active-row value that nothing set until a pointer touched it, so the entire task list, its checkboxes, menus and rename were unreachable by keyboard, and the pane-scoped j/k bindings could never fire because focus could not get in; `complete` read the open-subtask count only from loaded lists, so completing a parent opened by address skipped the decision-P1 dialog and silently archived every subtask; `workerChanged` bound 100 ids plus the owner into one statement, one over D1's 100-parameter limit, so a tree archive of 100 tasks rejected the internal event and left every open browser showing tasks the worker had already archived; the detail map was never pruned, so a reconnect after an afternoon's work fired ~60 concurrent detail reads at a 2 req/s lane; and the archive detail disowned any record not on the current listing page, including one it had just restored.
- 2026-09-16: The D1 merge is running: sequential `--no-ff` merges in the order workspace, search, access, documents, with a typecheck between each, followed by an independent verifier whose first duty is to prove no branch lost work. Seven hazards are named in the merge order — the `shell/workspace.tsx` and `globals.css` hotspots, two lockfile additions, the new rule that `packages/contracts` may import `z` only from `common/zod.ts` (the browser CSP has no `unsafe-eval`, so that module's `jitless` configuration must load first), the reshaped task-tree schemas, and the access branch's session-hint redirect in `proxy.ts`. Three defects are assigned to the merge because no single worktree could fix them: keyboard remapping is inert without the workspace branch's `ActionsProvider` seam, the palette's recent-tasks path fans out to eight parallel reads against an endpoint only the workspace branch has, and `InviteAdminService.list` costs about twenty D1 requests per admin search.
- 2026-09-16: **Phase D1 merged.** The four feature branches are in `feat/symplist-build` as four `--no-ff` merges in the order workspace, search, access, documents, each installed, typechecked and tested before the next.
  - Conflicts, and how each was resolved:
    - `apps/web/src/components/shell/workspace.tsx` — both branches had independently fixed the same crash, where the panel collapse sync called a panel's imperative API in the commit that mounted it. The workspace's `whenPanelReady` is the stronger of the two (it probes registration with `getSize()` and retries for ten frames, where search's `syncPanelCollapsed` retried once), so it is the one kept and search's now-redundant helper was removed rather than left dead beside it.
    - `packages/db/src/migrations.test.ts` and `schema.test.ts` — all four branches had widened the same two assertions for their own migration range. The merged file keeps the strongest form of each: every file inside the foundation or a feature range, every migration expand-only, every foundation table present exactly once and in order, and every table in the database `STRICT`.
    - `apps/web/src/app/(app)/layout.test.tsx` — workspace stubs the workspace api so the shell leaves its loading state, access stubs `GET /v1/me` so the gate admits the account. Both are needed and both were kept.
    - One repair outside the conflicts: decision S18's own row in `decisions.md` contained a literal NUL where it meant to write the `\0` escape, which made git and grep treat the whole ledger as binary — the exact defect that row exists to record. The two characters are now text; no row was touched otherwise.
    - `docs/build/decisions.md` — merged by `merge=union` as designed; every branch's rows are present (WS1–WS27, S1–S21, AC1–AC22, DOC.1–DOC.21), and the merge's own rulings are appended as M1–M6.
    - Migrations did not collide: each feature owns its own hundred (§3.4), so the merged directory is 0001–0018, 0100–0102, 0200–0202, 0300–0304 and 0400, with no duplicate and no gap inside a range.
  - Three defects only the merged tree could show, each found by a gate rather than by reading:
    - Every contracts module the search, access and documents branches added imported `z` from `"zod"` directly, which `common/zod.test.ts` refuses — the browser CSP has no `unsafe-eval`, so `common/zod.ts` and its `jitless` configuration must be the only source. Five files repointed; the test was not touched.
    - `POST /v1/tasks/:id/complete` answered 500 for every completion. The search branch's `searchArchiveContributor` carried its own write-id guard, written against an earlier shape of the archive seam, and the workspace branch's runner refuses any contribution without `archiveGuard`. It was also redundant: `tasks/plans.ts` now records one `search_intents` upsert per archived **and** promoted task in the deciding batch, so the contributor would have written every archived task's intent twice. The tasks domain is now the single writer and the contributor keeps its place in the seam contributing nothing.
    - `packages/core/src/search/contributors.test.ts` built an `ArchiveInput` without the `stopRun` and `archivedTaskIds` the workspace branch added to that interface.
  - The three cross-branch items the stages could not fix alone are closed:
    - **Keyboard remapping is live end to end.** The workspace feature reads the account's `keyboard` group and offers it as the shell's `keyboardPreferences` slot; the shell hands it to `ActionsProvider`; search's dispatcher resolves `effectiveBindings` against it. Both halves passed on their own while every remap silently did nothing, so `(app)/layout.test.tsx` now drives the whole chain — an account that remaps `shell.go_later` to `g b` navigates on `g b` and not on `g l` — and cutting the `preferences` prop out of `AppShell` fails it. `workspace.spec.ts` covers the same journey in a browser.
    - **The palette no longer fans out over recent tasks.** `createSearchApi.recentTasks` resolved up to eight recent ids with eight parallel `GET /v1/tasks/:id`, every time the palette opened on an empty query. It now places them from the owner's tree through `GET /v1/tasks`, one bounded page at a time, stopping the moment every id is placed and never reading more than four pages; the api serves all of them from the one owner tree it caches for 60 seconds, so the usual opening is one request over one D1 read.
    - **`InviteAdminService.list` is bounded.** One admin search read 200 rows at a time and loaded a key ring per page — up to twenty D1 requests. It now reads at most four 500-row pages and loads one key ring for all of them: at most five requests whatever the table holds, two for a table that fits in a page, with reach, order and the continuation cursor unchanged.
  - `pnpm e2e` had never been run across all four features, and it found five more integration defects that no branch could have seen:
    - `access.spec.ts` was refused at load — `async (_fixtures, testInfo)` is not an object destructuring pattern, so Playwright rejected the whole file before a single case ran.
    - Every `shell.spec.ts` and `search.spec.ts` case failed, because the access feature's gate correctly replaces the shell with sign-in for a caller `GET /v1/me` reports as signed out, and those specs deliberately never sign in. They now stub the identity alone, through one shared helper, and leave every other read to fail or to their own fixtures — which is the state each is actually about.
    - The suite's shared api started with `NODE_ENV=development`, so the test-only OTP outbox did not exist and no access journey could read an emailed code. `access.spec.ts` had been starting an api of its own with the right environment; once the workspace feature gave the whole suite one, the two definitions had to agree and did not. `helpers/local-api.ts` now owns that environment alone, including `ADMIN_BOOTSTRAP_EMAIL`, and the spec's private launcher is gone.
    - The bootstrapped administrator had never finished onboarding, so every administration route sent it back to `/welcome`; and the relock journey reloaded the invite screen it happened to be on instead of reopening the account, so "Restore access" was never there. Both were bugs in a spec that had never been executed.
    - One real accessibility defect: on the paused screen, "manage or delete your account" sits inside a sentence of muted text at 1.06:1 against it and was underlined only on hover — WCAG 1.4.1. It is underlined at rest now.
  - Every gate passes from `/Users/tejassuds/projects/symplist` on `feat/symplist-build`: `pnpm install` and `pnpm install --frozen-lockfile`; `pnpm lint` (1,102 files, zero errors, zero warnings); `pnpm typecheck`; `pnpm test` (3,634 Vitest tests across 16 projects plus 47 script tests, 9 skipped for missing credentials); `pnpm build` and `pnpm build:web:clean`; `pnpm e2e` (92 passed, 19 skipped); `node scripts/check-api-deploy.mjs` (30 migrations) and `python3 scripts/check_docs.py`.
  - Carried into D2, recorded rather than hidden: search indexes task titles only until the documents, Simon, preferences and scheduling source contributors are filled, and `SearchQueryService.invalidateChatOptIn` and the hourly re-enqueue of stale intents have no callers; `InboxFind` is built and tested but nothing mounts it, so `/` reports "This view has nothing to search" outside `/search`; the documents artifact viewer, share dialog and grant list are the `artifact-surface.ts` seam; Settings → Account leaves an empty Privacy slot for analytics; the six non-Account settings pages other features own are still stubs; and the documents surfaces have no Playwright coverage.
- 2026-09-16: **Phase D1 done.** The verification the crashed session never finished was completed directly in the main checkout, with no workflows: every gate passes on the merged tree — `pnpm install --frozen-lockfile`, `pnpm lint` over 1,102 files with zero errors and zero warnings, `pnpm typecheck` across every project, `pnpm test` at roughly 3,500 tests with no failures (web 1,476, api 377, core 267, crypto 302, config 276, db 164, email 145, analytics 104, search 97, testing 82, worker 80, docs 71, storage 40), `pnpm build` and `pnpm build:web:clean` over 34 routes, `pnpm e2e` at 92 passed and 19 skipped for other viewports, `check-api-deploy` over 12 packages and 30 migrations, and `check_docs` over the 44 screen briefs. Committed as 521c78f with the cross-feature fixes that verification produced: the e2e session and identity helpers, the axe helper, the access, search and shell specs, an api documents test and the paused access gate — none of which any single worktree could have exercised, because each was built where the other features did not exist.
- 2026-09-16: The repository is one clean checkout again. All four `wip/*` branches were fully absorbed by the merge, so their worktrees were removed and the branches deleted, reclaiming 5.6 GB; `git worktree list` now shows only the main checkout, and the branch list is `feat/symplist-build` and `main`. [phases.md](phases.md) records what D2, E, F and G each need, measured against the current tree rather than the plan: every D2 feature area is still a seam of between 4 and 36 lines, against 34 to 72 files for each D1 feature.
- 2026-09-16: Brand identity landed and the quick-add keys were split (decisions BR1, BR2, WS25). Also recorded honestly: `pnpm e2e` failed once per run for three consecutive runs during this session, on a different test each time, and passed 92/92 when the same suite was run directly with the line reporter and 95/95 afterwards. The failures were parallel-worker contention on a machine that had been running heavy builds, not a regression — but if a single unexplained e2e failure appears in CI, this is the first thing to suspect rather than a real break.
